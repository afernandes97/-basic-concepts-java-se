package fundamentals.primitiveTypes;

public class PrimitiveVsObject {
    public static void main(String[] args) {
        String s = "test text";
        s = s.toUpperCase();

        //with an object we can access the properties using a "." , except this primitive variables: byte, short, int, long, double, float, boolean, char.

        //Wrappers: is object version of primitive types, to more explications access the class: Wrappers.
    }
}
